language: python
python:
  - "3.3"
  - "3.4"
  - "nightly"

services:
  - redis-server

# command to install dependencies
install:
  - "pip install -r requirements.txt"
# command to run tests
script: py.test